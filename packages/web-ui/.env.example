# Base URL for the FastAPI backend.
# Default: same-origin (/api), works when served by `pick-face-web serve`.
# For dev with vite proxy issues (e.g. SSE buffering), point directly at FastAPI:
#   VITE_API_BASE=http://localhost:8000/api
VITE_API_BASE=/api